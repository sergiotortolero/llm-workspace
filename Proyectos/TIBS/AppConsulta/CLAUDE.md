# AppConsulta

## Type
Work / TIBS — **Development** (lead: Héctor Esparza).

## Status
The actual **AppConsulta application lives elsewhere** — there is **no app code here**.
This folder only contains a throwaway **iOS provisioning utility** built to answer a one-off
technical question. Per Sergio, it is **kept as reference only** (do NOT delete).

## What's in this folder (disposable utility)
A Python 3 + OpenSSL script (`leer_mobileprovision.py`) that reads an Apple
`embedded.mobileprovision`, extracts the internal plist, and lists provisioned device UDIDs.
Kept only as a placeholder for the AppConsulta initiative's context.

## Open questions
- [ ] Where does the real AppConsulta app live, and what is its scope/role at TIBS?
- Local provisioning utility: **kept as reference** (resolved — not deleted).

## Rules
- Spanish in conversation; code/docs/PRD in English.
