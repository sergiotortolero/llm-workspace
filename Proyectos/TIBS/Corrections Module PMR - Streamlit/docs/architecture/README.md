# Architecture intake — PMR Corrections Module

Coloca aquí el **documento de análisis y arquitectura** que mencionaste (el que resuelve
audiencia, dónde viven los datos, si se requiere grid editable "rico", y autenticación).
Cualquier formato sirve: pégalo como `.md` o deja el archivo aquí.

Cuando esté aquí, el agente `streamlit-migrator` podrá:
1. Resolver las decisiones abiertas de `../adr/0001-react-to-streamlit-evaluation.md`.
2. Generar el **plan de migración por fases** (PRD + ADR).
3. Empezar por **portar el motor de reglas de validación** a Python (lo más limpio).

> ⚠️ No guardes secretos/credenciales aquí (connection strings, tokens, contraseñas).
